{
  "name": "mvc",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "dependencies": {
    "@types/jquery": "^3.3.31",
    "jquery": "^3.5.0",
    "parcel": "^1.12.4"
  },
  "scripts": {
    "build": "yarn prebuild && parcel build src/index.html --public-url ./",
    "prebuild": "rm -rf dist",
    "start": "parcel ./src/index.html"
  }
}
